package templates

templ DashboardPage(email string) {
	@AppShell("Dashboard", email) {
		<h1 class="text-2xl font-semibold text-gray-900 dark:text-white">Dashboard</h1>
		<p class="mt-4 text-gray-700 dark:text-gray-300">Signed in as { email }.</p>
	}
}
