DATABASE_URL=postgresql://user:password@localhost:5432/flashcard

# Set to true in production (HTTPS) so session and CSRF cookies get the Secure
# attribute. Leave false for local plain-HTTP development.
SECURE_COOKIES=false

